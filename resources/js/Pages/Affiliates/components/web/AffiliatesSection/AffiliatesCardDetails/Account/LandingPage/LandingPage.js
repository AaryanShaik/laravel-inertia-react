import React from 'react';
import { Button } from 'antd';

const LandingPage = () => {
    return (
        <div style={{width:'100%'}}>
            <div style={{width:'100%',display:'flex',justifyContent:'center',alignItems:'center'}}>
            <Button type={'primary'}>Create Landing Page</Button>
            </div>
        </div>

    )
}

export default LandingPage
